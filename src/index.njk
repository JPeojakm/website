{% extends '_templates/base.njk' %}

{% block content %}
<div class="container">
  <div class="row justify-content-center">
    <div class="col col-md-8 col-lg-8">
      <div class="intro">
       <h1>🤖 Jinpeng Li aka J.P.</h1>
       <p>is a technology reporter based in Austin, TX. My passions include crafting <a href="your-link-for-maps" class="special-link">maps</a>, designing <a href="your-link-for-infographics" class="special-link">infographics</a>, making <a href="your-link-for-data-visualizations" class="special-link">data visualizations</a>, and I also love <a href="your-link-for-infographics" class="special-link">writing</a>. I create a newsletter called "<a href="https://theabacus.beehiiv.com/" class="special-link">The Abacus</a>" which focuses on AI development in Asia. Know more about my works below.</p>

       <p>#Longhorn #NewNewHuman #WeekendCooking</p>
     </div>
   </div>
  </div>
</div>

<div class="container-fluid">
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-4 g-4">
      {% for project in home.projects %}
      <div class="col">
        <a href="{{ project.source }}" class="text-decoration-none text-dark">
        <div class="card h-90 border">
          <img src="/img/projects-photo/{{ project.image }}" class="card-img-top" alt="{{ project.title }}">
             <div class="card-body">
              <h5 class="card-title">{{ project.title }}</h5>
              <div class="badge-container">
                  <span class="badge rounded-pill bg-primary">{{ project.credit }}</span>
                  <span class="badge rounded-pill bg-warning">{{ project.label }}</span>
              </div>
            </div>
        </div>
        </a>
      </div>
      {% endfor %}
    </div>
      </div>
{% endblock %}